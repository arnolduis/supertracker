#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../supertracker/bin/supertracker" "$@"
  ret=$?
else 
  node  "$basedir/../supertracker/bin/supertracker" "$@"
  ret=$?
fi
exit $ret
